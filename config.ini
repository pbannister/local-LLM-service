# ==============================================================================
# GLOBAL DEFAULT SETTINGS
# Models are auto-discovered from huggingface cache.
# Target: 16GB VRAM (AMD MI25). --fit auto-reduces ctx-size if needed.
# ==============================================================================
[*]
fit = on
#ctx-size = 65536
ctx-size = 0
mmap = 1
